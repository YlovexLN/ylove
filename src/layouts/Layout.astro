---
import "@/styles/global.css";

interface Props {
  title?: string;
  description?: string;
}

const { title = "YLOVEXLN", description = "YLOVEXLN 的个人主页与作品集" } = Astro.props;
---

<!DOCTYPE html>
<!-- 站点内容为中文：默认语言标识固定为 zh-CN（无 JS 时同样生效） -->
<html lang="zh-CN">
  <head>
    <meta charset="utf-8" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="description" content={description} />
    <title>{title}</title>
    <!--
      语言标识自动匹配：读取浏览器语言列表，命中中文标签时把 <html lang>
      精确为该变体（如 zh-Hant / zh-TW / zh-HK）；非中文浏览器保持默认 zh-CN，
      因为页面内容始终是中文。使用 is:inline 保证在首次渲染前同步执行。
    -->
    <script is:inline>
      (() => {
        const languages =
          navigator.languages && navigator.languages.length
            ? navigator.languages
            : [navigator.language];
        const matched = languages.find(
          (lang) => typeof lang === "string" && /^zh\b/i.test(lang)
        );
        if (!matched) return;
        // 统一为 BCP-47 写法（zh_Hant → zh-Hant）
        const normalized = matched.replace(/_/g, "-");
        if (normalized.toLowerCase() !== "zh-cn") {
          document.documentElement.lang = normalized;
        }
      })();
    </script>
    <!-- 预加载字体：避免 font-display:swap 期间先显示浏览器系统字体 -->
    <link rel="preload" as="font" type="font/ttf" href="/fonts/ChillRoundFRegular.ttf" crossorigin />
    <link rel="preload" as="font" type="font/ttf" href="/fonts/ChillRoundFBold.ttf" crossorigin />
  </head>
  <body>
    <slot />
  </body>
</html>
